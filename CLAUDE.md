# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

**fusion-executor is being built — Rust+PyO3 core via maturin.** P4 (fe-gui macOS GUI) **COMPLETE**: `fe-gui` (`accessibility` 0.2 safe wrapper for AXUIElement tree/focus/click/type/inspect + 3 audited unsafe FFI blocks `AXIsProcessTrusted` + `AXValueGetValue` ×2; CoreGraphics `CGDisplay::screenshot` → PNG base64 Layer-B fallback; crate-level `#![allow(unsafe_code)]` override — rustc 1.96 `unsafe_extern_blocks` makes `AXIsProcessTrusted()`/`AXValueGetValue()` unsafe-to-call, no crate choice avoids it; other 7 crates keep `unsafe_code="deny"`; user-approved scoped exception); `GuiAction` (serde tag=`kind`, rename_all=`snake_case`: `focus_app`/`click`/`type_text`/`key_press`/`screenshot`/`inspect_tree`) + `GuiResult{ok,node_tree,screenshot_png_b64,error}`; 6 Rust unit tests) wired into `fe-core` (`gui: GuiController` field + `gui_action()` method, re-exported `fe_core::gui`) → `fe-ipc` (`executor.gui_action` live, not stub — `-32013` AX-not-trusted, `-32600` bad-action deserialize) → `fe-pyo3` (`NativeGuiResult` pyclass + `gui_action(action: &PyAny)` via `py.import("json").dumps` → `serde_json::from_str`) → Python (`GuiResult` Pydantic model + `FusionSandboxExecutor.gui_action(action: dict) -> GuiResult`); 6 Python GUI tests (model roundtrip / key_press wire-name unsupported / bad-kind degrade / screenshot TCC-skip guard / UDS roundtrip / bad-kind UDS `-32600`). 67 Rust + 26 Python tests green, clippy/fmt/ruff clean, `maturin develop` builds. Smoke: trusted machine `gui_action({"kind":"screenshot"})` → valid 3.2MB PNG, `gui_action({"kind":"key_press","key":"Tab"})` → `ok=False` unsupported. Next: P5 (Hardening — criterion benches <5ms init + <3% truncation CPU, pytest --cov >80%, final docs). **P5 COMPLETE**: criterion benches — `Executor::new` 735µs (<5ms ✓), `truncate_output` ~1.75GB/s, `validate` 10k-compound 317ns (approximate, dev M-series; NFR 阈值 <5ms/<3% 是 SLA, 此数值非硬保证); Python coverage 95% (cli 96% / executor 90% / models 100%) via in-process `cli.main()` tests; clippy `--all-targets -D warnings` clean (only upstream `block v0.1.6` future-incompat notice), fmt/ruff clean; `truncate_output`→pub, `ClickCandidate` type alias, `as u32` removed for clippy. 67 Rust + 32 Python tests green, maturin builds. **v1 COMPLETE (P1–P5).** **v1.1 COMPLETE (KeyPress):** `fe-gui::key_press` — CGEvent synthesis (`CGEventSource::new(CombinedSessionState)` → `CGEvent::new_keyboard_event(src, code, keydown)` + keyup → `.post(CGEventTapLocation::HID)`), core-graphics 0.24 safe wrapper encapsulates the unsafe FFI (zero hand-written unsafe blocks; reuses fe-gui crate-level `#![allow(unsafe_code)]` scope). `resolve_keycode` maps 40+ case-insensitive key names + aliases (enter=return, esc=escape, up=up_arrow, cmd=command...) → `core_graphics::event::KeyCode` constants (Return/Tab/Space/Delete/Forward_delete/Escape/arrows/Home/End/PageUp-Down/Help/F1-F20/Command/Shift/Option/Control/Function/Caps_lock/Mute/Volume). Unknown key → `ok:false` + `unknown-key: ...` (no panic, trusted-independent). +2 Rust unit tests (keycode mapping / unknown None) + KeyPress degrade test rewritten; +1 Python test (`keypress_when_trusted`, TCC-skip). Smoke: trusted `gui_action({"kind":"key_press","key":"Tab"})` → `ok=True`, `{"key":"return"}` → `ok=True`, unknown → `ok=False` unknown-key. 69 Rust + 33 Python tests green, clippy/fmt/ruff clean, `maturin develop` builds. See `~/.claude/plans/nifty-foraging-lark.md` for the approved design + build phases (P1–P5). Verify against real files before relying on any structure here. **v1.2 COMPLETE (Streaming + Modifiers + Screenshot dimensions):** (1) Live stdio streaming wired through 4 layers — fe-sandbox `run_streaming` (mpsc `StreamEvent::Chunk{data}`/`Done(SandboxResult)`) → fe-core `execute_streaming(req) async -> Result<(mpsc::Receiver<ExecutionStreamEvent>, JoinHandle<()>)>` (validates; blocked→single Done frame; snapshot; run_streaming; spawned task forwards chunks + on Done applies diagnostics slice + maps to `ExecutionResult`; `ExecutionStreamEvent` enum `#[serde(tag="type", rename_all="snake_case")]` `Chunk{data}`/`Done(ExecutionResult)` — serde FLATTENS Done into `{"type":"done", exit_code:..., ...}` not nested) → fe-ipc `executor.execute_stream` (multi-frame NDJSON, id reused: chunk `{jsonrpc,id,result:{type:chunk,data}}` + done `{jsonrpc,id,result:{type:done,result:{ExecutionResult}}}` — UDS path nests done under `result.result`, DIFFERENT shape from fe-pyo3 flatten, deliberately separate) → fe-pyo3 `NativeStreamIterator` pyclass (`__iter__(slf: Py<Self>)->Py<Self>` + `__next__` via `py.detach(|| ...)` + `BLOCKING_RT.block_on(rx.recv())` + `serde_json::to_string`→`py.import("json").loads`) → Python `run_streaming` generator (yields chunk strings then `ExecutionResult` at done; `_native_result(frame)` passes frame DIRECTLY since serde flatten). (2) KeyPress modifier combos — `key` single key name + `modifiers` array (cmd/ctrl/alt/shift/fn); CGEvent flag synthesis, single-key only no chord. (3) `GuiResult` adds `screenshot_width`/`screenshot_height` (u32, PNG pixels) from `CGImageRef`. pyo3 0.29 API: `allow_threads` REMOVED → `py.detach(|| ...)` (closure 0 args); `PyObject` REMOVED → `Py<PyAny>`; `py.import("json").call_method1("loads",(s,))?.unbind()`. `BLOCKING_RT` = `LazyLock<tokio::runtime::Runtime>` (multi-thread); streaming `execute_streaming` is async, PyO3 `block_on` to get (rx,handle), `__next__` `block_on(rx.recv())` same runtime no nesting panic. +4 fe-core tests (echo chunk/done, blocked single frame, timeout -124, diagnostics ValueError) +2 fe-ipc tests (stream chunks/done over UDS, blocked single frame) +5 Python tests (streaming echo/blocked/timeout/diagnostics + UDS stream). 81 Rust + 40 Python tests green, clippy/fmt/ruff clean, maturin builds. **v1.3 COMPLETE (Data Schema §4.1 + Native File Tools + Surgical Patch Engine):** PRD gap analysis found 3 missing work categories — all landed. (Gap #1) `ExecutionResult` gains `task_id`/`command`/`duration_sec` (PRD §4.1) across 4 layers (fe-core serde struct → fe-pyo3 `NativeExecutionResult` → Python Pydantic → fe-ipc done frame); `blocked_with(reason, task_id, command)` backfills request-side identity on security-block (duration 0.0). (Gap #2) NEW crate `fe-tools` — `file_edit` (unique-match exact replace via `content.matches(old_string)`: 0→NoMatch, >1→Ambiguous reject, ==1→`replacen`+atomic write `.fe-tmp-{pid}`+rename); `glob` (`glob::glob` with `cwd_abs = canonicalize(base)` computed BEFORE pattern build — the fix for relative-path bug where canonicalized macOS temp-dir symlink results failed `strip_prefix`; returns relative paths); `grep` (`Regex` + walkdir recursion skipping dotfiles + binary `\0` detection + 1000-hit cap). All reuse `fe-security::SecurityGuard::validate_cwd` via `guard_path` (expand_tilde, join cwd if relative, reject `..` escape via canonicalize starts_with, validate parent dir). (Gap #3) Surgical Patch Engine in `fe-tools`: `apply_patch` (diffy 0.4.2 `Patch::from_str` → loop `patch.hunks()` counting via `count_hunk_lines` matching `diffy::Line::Insert`/`Delete`/`Context` variants — Line is an enum NOT `.starts_with()`; full-rewrite heuristic `pf.new_range().start()==0 && end()==0` → `FullRewriteForbidden`; target = `patch.modified().or(original())` with `strip_prefix("b/")`/`("a/")`; `diffy::apply(base, &patch)`); `replace_function` (`parser_for_ext` + `function_node_kind` for py/js/ts|tsx/rs + **stack-based preorder traversal** — tree-sitter 0.25.10 has NO `descendants()`, and `Node::children(&mut cursor)` borrows the cursor mutably so recursive shared-`&mut cursor` fails with lifetime/borrow errors; fix = iterative `stack.push(root)`, per-node local `cursor` + `node.children(&mut cursor).collect()` into Vec, drop cursor, push children reversed; `child_by_field_name("name")` for function name; byte-slice replace `[..span.start]+new_body+[span.end..]`; no grammar → `locate_function_regex` fallback). 4-layer wiring: fe-core `tools: Tools` field + 5 wrapper methods → fe-ipc 5 arms (`executor.file_edit/glob/grep/apply_patch/replace_function`, cwd optional, paths as `Vec<String>`) → fe-pyo3 `NativeEditResult`/`NativeGlobEntry`/`NativeGrepMatch` pyclasses (From<RsX>) + 5 `#[pymethods]` → Python `EditResult`/`GlobEntry`/`GrepMatch` Pydantic + 5 `FusionSandboxExecutor` methods. clippy fix: `ExecutionStreamEvent::Done(ExecutionResult)` → `Done(Box<ExecutionResult>)` (clippy `large_enum_variant` — Done 264B vs Chunk 24B; serde transparent over Box, wire format unchanged; 3 construction sites wrapped `Box::new`, match sites unchanged via deref). +13 fe-tools Rust unit tests + 12 Python tests (incl. file_edit/glob UDS roundtrip using subprocess `server` fixture pattern — NOT in-process `serve()` which blocks forever on a daemon thread and hangs pytest). 94 Rust + 52 Python tests green, clippy `--all-targets -D warnings` clean (only upstream `block v0.1.6` future-incompat), fmt/ruff clean, maturin builds. **v1.4 COMPLETE (Auto-rollback + Live Telemetry + GUI scroll/drag/wait):** (1) Auto-rollback (FR-04 optional strategy) — fe-core `AutoRollbackGuard` + `RollbackPolicy{max_consecutive_failures (reserved field), file_damage_check}`: on `exit_code!=0` + `git status --porcelain` non-empty → `rollback(本次快照)` + `result.auto_rolled_back=True`; guard lifecycle limited to single execute (Executor stays stateless — consecutive-failure counting owned by caller self-healing loop, NOT executor); 4-layer wiring (fe-core `execute_async`/`execute_streaming` construct guard before sandbox.run → fe-pyo3 `auto_rollback_policy` dict→serde via `serde_json::from_str` → Python `RollbackPolicy` Pydantic + `run()`/`run_streaming()` kwargs); fe-rollback HEAD-baseline bug fix (stash SHA != HEAD → `git stash apply`; equal → skip, return Ok(true) — was wrongly trying `stash apply HEAD` which is a commit not a stash). (2) Live telemetry — NEW crate `fe-telemetry`: `TelemetrySample{ts_ms,cpu_pct,mem_mb,gpu_pct?,gpu_mem_mb?,task_id?}` (GPU fields `Option` + `skip_serializing_if` — executor runs no model, no GPU handle, caller-injected) + `TelemetryConfig{interval_ms=100 (10Hz), max_samples=0 (infinite)}`; `start_stream(cfg, rt::Handle)` spawns sysinfo sampling task ON the passed runtime handle (NOT bare `tokio::spawn` — panicked "no reactor running" when called from sync PyO3 entry without runtime context; fix = `BLOCKING_RT.handle().clone()` passed from fe-core); sampling uses `sys.refresh_processes_specifics(ProcessesToUpdate::Some(&[pid]), false, ProcessRefreshKind::new().with_memory().with_cpu())` (3-arg, NOT 2-arg `refresh_processes`; `System::new()` does NOT populate processes); first frame sleeps then samples (cpu_usage needs refresh interval); channel close / max_samples reached → stop. 4-layer wiring: fe-core `Executor.telemetry_stream(cfg) -> (mpsc::Receiver, JoinHandle)` spawns on `BLOCKING_RT.handle()`, re-exports `fe_telemetry as telemetry` + `TelemetryStreamConfig`/`TelemetryFrame` aliases → fe-ipc `executor.telemetry_stream` multi-frame NDJSON dispatch (each frame `{type:sample, sample:{...}}`, reuses req id; handler parses `interval_ms`(100)/`max_samples`(0)) → fe-pyo3 `NativeTelemetryIterator` pyclass (`__iter__`/`__next__` via `py.detach` + `BLOCKING_RT.block_on(rx.recv())` + `serde_json::to_string`→`json.loads`; StopIteration on channel close; **never-type fallback fix** — rustc 1.96 `#[deny(dependency_on_unit_never_type_fallback)]` rejected `serde_json::to_string(&sample)` where `sample` came from `Option::map` fallback; fix = explicit `let sample: TelemetrySample = match ...` annotation; same fix applied to `PyStreamIterator.__next__` `let ev: ExecutionStreamEvent`) → Python `TelemetrySample` Pydantic + `FusionSandboxExecutor.telemetry_stream(interval_ms=100, max_samples=0)` generator (yield `TelemetrySample`, `enumerate` for count). fe-pyo3 import fix: `TelemetryConfig`/`TelemetrySample` are private in fe-core (re-exported only as `TelemetryStreamConfig`/`TelemetryFrame` aliases), import via `fe_core::telemetry::{TelemetryConfig, TelemetrySample}` path instead. (3) GUI scroll/drag/wait (fe-gui CGEvent synthesis): `scroll` (dx/dy pixels via CGEvent scrollWheel unit axis) / `drag` (from x,y → to x,y: mouseMove→leftMouseDown→move→leftMouseUp) / `wait` (seconds sleep, test helper). +3 fe-telemetry Rust unit tests (stream produces samples / stops on rx drop / sample serializes) +1 fe-core (telemetry_stream yields 4 + stops, gpu_pct None) +1 fe-ipc (UDS telemetry 3 frames, type==sample, ts_ms sequence) +5 Python auto-rollback tests +3 Python telemetry tests (native iter / wrapper / UDS 3-frame). 105 Rust + 60 Python tests green, clippy `--all-targets -D warnings` clean (only upstream `block v0.1.6` future-incompat), fmt/ruff clean, maturin builds. **v1.5 进行中 (诊断切片扩语言 TS/Go):** fe-diagnostics `Slicer` 从 4 正则扩到 9 正则 — 新增 `ts_re` (tsc 括号形式 `file.ts(l,c): error TSxxxx: msg`), `ts_dash_re` (tsc watch 冒号-短横 `file.ts:l:c - error TSxxxx: msg`), `bun_re` (Bun 运行时小写 `error: msg` + 裸 `at path:line:col` 无括号, 现有 node_re 只匹配 V8 大写 `Error:` + 带括号), `go_panic_re` (`(?s)` 跨行 `panic: msg\n...goroutine N [running]:\n...\n\tfile.go:line`, 取末栈帧), `go_compile_re` (`file.go:l:c: msg` 无 `error:` 关键字 — swift_re 因要求 `error:` 漏此格式). 新增 `extract_ts` (两正则 fallback + `build_ts_diag` helper)/`extract_bun`/`extract_go_panic`/`extract_go_compile` 方法. `slice()` 排序: ts→python→node→bun→rust→go_panic→swift→go_compile (TS 前置因 tsc 输出独有; go_compile 末位因扩展名 `.go` 隔离不冲突). 互不冲突: 各正则按扩展名/关键字隔离. 零新依赖 (slicer 纯文本行切片, `_parser_for_ext` 死代码不动). 关联 fe-security: 白名单加 `go`+`tsc` (否则真实 `go build`/`tsc --noEmit` 被 Stage-2 拦截无法 E2E; node 已在白名单跑 tsc shim) +1 白名单测试. +5 fe-diagnostics 单元测试 (ts_compiler/ts_watch/bun_runtime/go_panic/go_compile). 真实工具链 E2E 验证: tsc 7.0.2 `bad.ts(2,5): error TS2322: ...` → `TS2322`/`bad.ts`/line 2 ✓; `go build ./...` `./main.go:6:5: undefined: foo` → `compile error`/`./main.go`/line 6 ✓; `go run panic.go` `panic: runtime error: index out of range [5]...` → `panic`/`panic.go`/line 7 ✓. 诊断无需 4 层 wiring — 已自动经 execute→ExecutionResult.diagnostics 流通 (exit_code!=0 时). 110 Rust + 60 Python 测试全绿, clippy `--all-targets -D warnings` 净, fmt/ruff 净, maturin 构建. **v1.5 #13 COMPLETE (GUI 更多动作):** fe-gui `GuiAction` 9→16 变体 — 新增 `double_click` (ax_label/ax_position, CGEvent 2× LeftMouseDown/Up, 第二击 `set_integer_value_field(EventField::MOUSE_EVENT_CLICK_STATE, click_no)` for click_no 1..=2; `EventField` 是 `pub struct` 非 enum, 常量为关联常量) / `right_click` (ax_label/ax_position, CGEvent RightMouseDown/RightMouseUp + `CGMouseButton::Right`) / `hover` (ax_position, CGEvent MouseMoved 无按键) / `window_close`/`window_minimize`/`window_zoom` (bundle_id, AX 按钮属性 `kAXCloseButtonAttribute`/`kAXMinimizeButtonAttribute`/`kAXZoomButtonAttribute` → `downcast::<AXUIElement>` → `press()` safe wrapper) / `window_resize` (bundle_id + width/height, **拖拽式** resize 非 AXValueCreate — 读 AXPosition+AXSize 算右下角 from/to 坐标, 复用已测 `drag()` CGEvent; AXValueCreate 设 AXSize 需新增 extern "C" unsafe block, 超出既定 3 处 unsafe scope, 拖拽走 safe wrapper 留在 scope 内). `resolve_click_position` helper 共用 click/double_click/right_click (ax_position 优先; 否则 ax_label→`focused_app`→`focused_window`/`main_window`→`find_click_target`→`read_position`; 无二者 `Err("需 ax_label 或 ax_position 定位点击坐标")`). **4 层 auto-flow 关键洞察**: fe-core/fe-ipc/fe-pyo3 在 `GuiAction` enum 级反序列化后 dispatch — 新变体零 wiring 自动流通 (仅 fe-gui 改 enum+execute+方法+测试); Python `gui_action(action: dict)` 通用无 per-variant 逻辑, GuiResult 结构通用. **0 新增 unsafe**: 全走 accessibility 0.2 safe wrapper (attribute/press) + core-graphics 0.24 safe wrapper (CGEvent/set_integer_value_field); 复用 fe-gui crate 级 `#![allow(unsafe_code)]` scope. `execute()` 闸门: Wait 先处理 (trusted-independent), 然后 `!ax_trusted()` → 统一降级 `ok=false, error="accessibility-permission-required"` (在 AX 树遍历前), 其余走 match dispatch. +5 fe-gui Rust 单元测试 (v15 变体 serde 往返 / snake_case / window 降级 early-return-if-trusted / pointer 降级 / no-target 双分支 trusted=Err/untrusted=degrade) +2 Python 测试 (`test_gui_action_new_variants_degrade_without_trust` CI 路径 7 变体断言降级 / `test_gui_action_pointer_variants_when_trusted` TCC 路径 hover+double_click+right_click 带坐标 ok=True; **TCC 权限分离修** — 原 `_ax_trusted()` 探 screenshot=Screen Recording, 但 Rust 降级闸门用 Accessibility; 两 TCC 权限独立, 机器可能 Accessibility trusted 但 Screen Recording untrusted → `_ax_trusted()=False` 但 pointer ok=True, 降级测试误跑; 新增 `_ax_access_trusted()` 探 hover=Accessibility, 降级+pointer 测试改用此 probe). smoke: trusted 机 hover/double_click/right_click 带坐标 → `ok=True` (CGEvent posted); window_* 需真实 GUI 会话 (AX 窗口树) 沙箱内降级 (`kAXErrorCannotComplete`/`element not found`). 116 Rust + 62 Python 测试全绿, clippy `--all-targets -D warnings` 净 (仅上游 block v0.1.6), fmt/ruff 净, maturin 构建. **v1.5 #14 COMPLETE (双向 IPC server-push):** fe-ipc `BroadcastHub` (Arc 共享) — IPC broker 关注点, **fe-core Executor 保持无状态** (广播扇出不进 executor)。registry: sub_id → `Subscriber{conn_id, channels: HashSet, tx: mpsc::Sender<Value>}`; conn/sub counter AtomicU64; `telemetry_task`/`screenshot_task` `Mutex<Option<JoinHandle>>` lazy 启停 (0 订阅自退, 下次 subscribe 重启; 源方法取 `&self` 非 `self: Arc<Self>` — 避自移后 `.take()` use-after-move)。连接 DUPLEX: `handle_conn` 拆 read_task (分发请求) + push_task (写 server-push 帧) 共享 `Arc<AsyncMutex<OwnedWriteHalf>>` (锁下行写原子); 每连接 `mpsc::channel<Value>(128)` push 帧 + oneshot close。三广播源: **telemetry** (单一 `executor.telemetry_stream` 扇出, 10Hz 默认 `interval_ms=100`) / **stdio** (execute/execute_stream 处理器调 `hub.broadcast_stdio()` 扇出 chunk/done — **跨连接** A 订阅 B 跑命令 A 收推送) / **screenshot** (周期 `gui_action(Screenshot)` 采样 `spawn_blocking`, `screenshot_interval_ms=1000` 慢于 telemetry; TCC 未授权 → 帧 data.ok=false 不崩, 复用 fe-gui safe wrapper)。推帧 `{"jsonrpc":"2.0","method":"executor.event","params":{"subscription_id":"sub-N","channel":..,"data":..}}` — **无 id** (notification 约定), 客户端按有无 id 区分响应 vs 推送; `collect_targets(channel)` 锁外 try_send (快照 sub_id+tx 后释放锁)。fe-ipc **0 新增 unsafe** (crate 仍 `unsafe_code="deny"`, 截图复用 fe-gui safe wrapper)。fe-pyo3 无改动 — server-push 需运行中 server, 纯 Python UDS 客户端 `Subscription` 连接 `serve()` socket; `Subscription._open()`/`__next__` (过滤 `executor.event` 匹配 sub_id, 跳非 event 帧)/`unsubscribe()`; `SUB_CHANNELS=("telemetry","stdio","screenshot")` 未知通道 `ValueError`; `FusionSandboxExecutor.subscribe(channels, *, sock_path, interval_ms, screenshot_interval_ms) -> Subscription`。+7 fe-ipc Rust 单元测试 (subscribe telemetry 推帧/missing channels -32600/unsubscribe 停推/stdio 跨连接广播 + 原有 health/unknown/malformed/execute/stream) +3 Python 测试 (telemetry 推帧/stdio 跨连接/未知通道 ValueError); 测试用 `UnixStream::into_split()` → `(OwnedReadHalf, OwnedWriteHalf)` 避借用后移。120 Rust + 64 Python (6 skip TCC) 测试全绿, clippy `--all-targets -D warnings` 净 (仅上游 block v0.1.6), fmt/ruff 净, maturin 构建. **v1.6 COMPLETE (#7 glob/grep ripgrep parity):** fe-tools `grep` 从 content-only 扩为三模式 ripgrep parity — 新增 `GrepOutputMode` enum (`Content`#[default]/`FilesWithMatches`/`Count`, snake_case, derive Default) + `GrepOptions{output_mode, after, before, context, multiline, glob_include, glob_exclude}` (全 `#[serde(default)]`) + `GrepOutput{output_mode, matches, files, counts}` (Vec 字段 `skip_serializing_if=Vec::is_empty`) + `GrepFileCount{path, count}` + `GrepMatch` 增 `context_before`/`context_after` (skip_serializing_if=Vec::is_empty). 5 缺口填齐: (1) files_with_matches 模式 (ripgrep -l) 仅返文件名去重; (2) count 模式 (-c) 每文件命中数; (3) context 行 -A/-B/-C (`context`=max(A,B), 仅 content 模式填 `context_before`/`context_after`); (4) multiline (ripgrep -U) 整文件 buffer `re.find_iter` 跨行匹配, `RegexBuilder.multi_line().dot_matches_new_line()` (坑: multi_line 只影响 ^/$ 锚点, `.` 跨行需显式 dot_matches_new_line); (5) glob include/exclude (-g) `globset::GlobSet` 过滤文件路径. glob() gitignore-aware: 旧版 `glob::glob` 不读 .gitignore → 改 `ignore::WalkBuilder` (hidden(false)/ignore(true)/git_ignore/git_exclude/git_global/parents/follow_links(false)/**require_git(false)** — 坑: 默认 require_git=true 仅 git 仓生效, fusion 子项目多为独立非 git 仓, 必须 require_git(false) 才在裸目录遵循 .gitignore; IGN_ODIRS 硬基线 + `add_custom_ignore_filename(".venv")` 防 10 万条目 OOM). 新依赖: `ignore 0.4` (gitignore-aware WalkBuilder) + `globset 0.4` (glob include/exclude). guard_path 坑: 返回 LITERAL joined path (`cwd.join(".")` = `/var/.../.` 带尾缀 `/.`), 非 canonicalize → walkdir 产 `/var/..././a.py`, `strip_prefix(cwd_abs=/private/var/...)` 失败; fix = grep_run 每 path `let root = std::fs::canonicalize(&abs).unwrap_or(abs.clone())` 用 root 做遍历前缀. 4 层 wiring: fe-core 增 `grep_with_opts` wrapper + re-export `GrepOptions as ToolsGrepOptions`/`GrepOutput`/`GrepOutputMode` → fe-ipc `executor.grep_with_opts` arm (manual `param_str` 提 pattern/paths/cwd + `params.get("opts")` serde_json::from_value or default) → fe-pyo3 `NativeGrepFileCount`/`NativeGrepOutput` pyclass (raw String + to_dict json.loads, getters output_mode/matches/files/counts) + `grep_with_opts(pattern, paths, opts: &Bound<PyAny>, cwd, py)` (json.dumps→serde_json::from_str) → Python `GrepFileCount`/`GrepOutputMode`/`GrepOptions`/`GrepOutput` Pydantic (_STRICT extra=forbid) + `FusionSandboxExecutor.grep_with_opts(pattern, paths, opts, *, cwd) -> GrepOutput`. +6 fe-tools Rust 单元测试 (files_with_matches/count/context/multiline/glob_filter/gitignore) +7 Python native 测试 +1 UDS roundtrip. 287 Rust + 152 Python (6 skip TCC) 测试全绿, clippy `--all-targets -D warnings` 净 (仅上游 block v0.1.6), fmt/ruff 净, maturin 构建.

## What fusion-executor Is

Controlled execution sandbox + macOS OS-level control hub for the Fusion ecosystem. Connects the inference engine (fusion-mlx) to system-side actions. The "hands" of fusion-code / fusion-agent — runs shell commands safely, drives native GUI via Accessibility API, and rolls back on failure.

Dual-mode controlled execution engine (CLI + Native GUI) for Apple Silicon / macOS. Sits at the **L4 generic-tool layer** of the monorepo. The architecture audit (`audit/fusion-ar-audit.md` P2-7) designates it as the home for the code-sandbox capability currently embedded in fusion-science (PythonExecutor/JupyterKernel/RExecutor) — that execution engine should be extracted here, not rebuilt per-vertical.

## Four Core Subsystems

1. **Security Guard Engine** — two-stage: static regex pattern match (fast filter for `rm -rf`, `sudo`, format, remote pipe) then shlex-style Lexer/AST tokenizer that splits compound shell commands (`&&`, `||`, `;`, `|`) and validates each binary against a whitelist (python, node, pytest, cargo, swift...). Intercept rate target: 100%.
2. **Subprocess / PTY Sandbox** — PTY or stdio subprocess to capture ANSI color codes + full Traceback; heartbeat timer enforces timeout (SIGINT graceful → 500ms grace → SIGKILL forceful, cleans whole child process tree via `killpg`; Issue #8: `timeout_sec == 0` is bounded to `DEFAULT_TIMEOUT_CAP_SEC` 120s, never truly unbounded); stdio truncation keeps head context + tail stack trace, folds the middle past `max_output_chars`.
3. **macOS Computer Use Adaptor** — `AXUIElement` to extract foreground-window UI node tree (button/textfield coords + Accessibility Label); falls back to `CGWindowListCreateImage` framebuffer capture for vision grounding (mlx-vlm / fusion-design) when nodes lack Accessibility info. Click/keystroke latency target <30ms.
4. **Task Rollback Manager** — Git HEAD snapshot before any file-mutating command; lightweight `snapshot_create()` / `rollback()` hooks supporting single-file `git checkout` (avoid clobbering unaffected modules); auto-rollback on detected file damage within a single execute (caller-driven: consecutive-failure counting stays with the caller's self-healing loop — Executor is stateless, `RollbackPolicy.max_consecutive_failures` is a reserved/deferred-by-design field, accepted on the wire but never read).

## Data Schema (Pydantic)

The wire contract — `ExecutionRequest` in, `ExecutionResult` out. `ExecutionResult` carries: `exit_code` (0 ok / -124 timeout / -1 blocked-or-internal-error), truncated `stdout`/`stderr`, `task_id`/`command`/`duration_sec` (PRD §4.1, v1.3 — request-side identity + wall-clock), `timed_out`, `blocked_by_security` + `security_reason`, `snapshot_id` for rollback. Diagnostics Slicer runs when `exit_code != 0`: regex-extract Traceback/Error/Exception lines → Tree-sitter AST to locate offending file:line → emit compact JSON (`error_type`, `file_path`, `line_number`, `code_snippet`, `raw_trace`) sized for a prompt.

v1.3 adds native file-tool wire models (fe-tools, replaces Claude SDK FileEdit/Glob/Grep): `EditResult{ok, path, error, matches}` (file_edit unique-match replace + apply_patch + replace_function all return this), `GlobEntry{path, is_dir}`, `GrepMatch{path, line_number, content}`. Surgical Patch Engine: `apply_patch` (diffy Unified Diff, full-rewrite forbidden) + `replace_function` (tree-sitter AST function-level replace, no full-file rewrite).

Issue #6 expands fe-tools with three file-edit parity features (all return `EditResult`, all RMW ops under flock + 64MB OOM cap + atomic_write): (1) `file_edit` gains `replace_all: bool` — `true` replaces every occurrence (Claude Code FileEdit parity), `false` (default) preserves the unique-match contract (`count>1` → degraded `ok=false`). (2) `multi_edit(path, edits, cwd)` — atomic all-or-nothing batch on one file: each `MultiEditItem{old_string, new_string, replace_all}` applied to an in-memory buffer in order; any item failure (no-match / ambiguous / empty old_string) discards the buffer, the file lock drops, NO write happens (caller-visible degraded `EditResult` with `第 N 项` index). (3) `notebook_edit(path, new_source, cell_id?, cell_number?, edit_mode, cwd)` — nbformat 4 `.ipynb` cell editing (parse via serde_json, no new heavy dep): `edit_mode` = `replace`|`insert`|`delete`, locate by `cell_id` (loop match) or `cell_number` (0-indexed range check); `source_to_lines` helper splits source into nbformat cell source (trailing `\n` except last line); missing/out-of-range cell → degraded `ok=false`, file untouched. 4-layer wiring: fe-core wrappers (re-export `MultiEditItem`/`NotebookEditMode`) → fe-ipc manual params extraction (file_edit `replace_all`, multi_edit `edits` array→`serde_json::from_value`, notebook_edit `cell_id`/`cell_number`/`edit_mode`) → fe-pyo3 `#[pymethods]` (file_edit `#[pyo3(signature)]` macro, multi_edit `py: Python` + `&Bound<PyAny>` + `json.dumps` pattern, notebook_edit `edit_mode` str→serde) → Python `MultiEditItem` Pydantic + `multi_edit`/`notebook_edit` methods. UDS methods: `executor.multi_edit`, `executor.notebook_edit`. 281 Rust + 145 Python tests green.

v1.7 adds `write_file(path, content, cwd)` (Claude Code Write parity, returns `EditResult`): atomic whole-file create/overwrite — `guard_path` + 64MB cap + `create_dir_all(parent)` + `FileLock` if exists + `atomic_write` (temp+persist). `file_edit` create-on-empty: missing path + empty `old_string` → delegates to `write_file`; missing + non-empty → reject; existing + empty → reject (L-TOOLS-02 preserved). `guard_path` literal-fallback bug fixed (raw cwd vs canonicalized cwd_abs on macOS tempdir symlink). 4-layer wired. 295 Rust + 165 Python green.

v1.4 adds auto-rollback + telemetry wire models: `RollbackPolicy{max_consecutive_failures (reserved), file_damage_check}` (optional `auto_rollback` kwarg on `run()`/`run_streaming()`; on failure + git-status-detected file damage → `rollback(本次快照)` + `result.auto_rolled_back=True`); `ExecutionResult` gains `auto_rolled_back: bool`. `TelemetrySample{ts_ms, cpu_pct, mem_mb, gpu_pct?, gpu_mem_mb?, task_id?}` (10Hz CPU/mem sampling, GPU caller-injected, `skip_serializing_if` omits None) + `TelemetryConfig{interval_ms=100, max_samples=0}` consumed by `telemetry_stream()` generator → UDS `executor.telemetry_stream` multi-frame `{type:sample, sample:{...}}`.

v1.5 expands the Diagnostics Slicer regex coverage from 4 to 8 languages: Python (Traceback), TypeScript (tsc `file.ts(l,c): error TSxxxx:` + watch `file.ts:l:c - error TSxxxx:`), Node (V8 `Error:` + paren), Bun (lowercase `error:` + bare `at`), Rust (panic), Go-panic (`panic: msg ... goroutine ... \tfile.go:line`), Swift (`file:l:c: error:`), Go-compile (`file.go:l:c: msg`, no `error:` keyword). Zero new deps — slicer is pure-text line extraction (tree-sitter grammars remain a reserved `_parser_for_ext` dead path). Security whitelist gained `go` + `tsc` so real `go build`/`tsc --noEmit` failures flow through the E2E pipeline.

v1.5 #13 adds 7 GuiAction variants (9→16 total): `double_click`/`right_click`/`hover` (CGEvent pointer synthesis, ax_label/ax_position) + `window_close`/`window_minimize`/`window_zoom`/`window_resize` (AX button attrs `press()` + drag-based resize). All via accessibility 0.2 + core-graphics 0.24 safe wrappers — 0 new unsafe (reuses fe-gui crate-level `#![allow(unsafe_code)]` scope). New variants auto-flow through fe-core/fe-ipc/fe-pyo3 (enum-level deserialize+dispatch, zero per-variant wiring); Python `gui_action(action: dict)` generic.

v1.5 #14 adds bidirectional server-push: `subscribe`/`unsubscribe` over UDS — `executor.subscribe` params `channels`(`["telemetry","stdio","screenshot"]`)/`interval_ms`(100)/`screenshot_interval_ms`(1000) → response `{ok, subscription_id:"sub-N"}`, then server pushes notification frames (no `id`, `method:"executor.event"`) `{"jsonrpc":"2.0","method":"executor.event","params":{"subscription_id":..,"channel":..,"data":..}}`. 3 broadcast sources in fe-ipc `BroadcastHub` (lazy start/stop, 0 subscribers self-exit): telemetry (single `telemetry_stream` fan-out 10Hz), stdio (execute/execute_stream fan-out chunk/done cross-connection), screenshot (periodic `gui_action(Screenshot)` via `spawn_blocking`, TCC-untrusted → `data.ok=false` frame no crash). Connection DUPLEX: read_task + push_task share `Arc<AsyncMutex<OwnedWriteHalf>>`. **fe-core Executor stays stateless** (fan-out is IPC broker concern). fe-ipc 0 new unsafe (crate keeps `unsafe_code="deny"`). Python `Subscription` pure-Python UDS client: `subscribe()` → `__next__` yields `executor.event` params matching sub_id → `unsubscribe()`; `SUB_CHANNELS` validation.

v1.6 (issue #12 gaps #12.1/#12.2): (1) `fe-gui` `GuiAction` 16→18 变体 — 新增 `triple_click` (ax_label/ax_position, CGEvent 3× LeftMouseDown/Up, `click_state` 1..=3; 复用 `resolve_click_position`) 与 `hold_key` (单 key name + duration_ms, CGEvent keydown→sleep→keyup; duration 截 5000ms 上限防挂死; 未知键名 `resolve_keycode` 返 None → `ok:false, unknown-key: ...` 降级, 信任无关). 4 层 auto-flow (enum 级 deserialize+dispatch, 零 per-variant wiring; fe-gui 改 enum+execute+方法+测试, 其余 3 层 + Python `gui_action(action: dict)` 通用). 0 新增 unsafe (accessibility 0.2 + core-graphics 0.24 safe wrapper, 复用 fe-gui crate 级 `#![allow(unsafe_code)]` scope). (2) `fe-diagnostics` 删死路径 `_parser_for_ext` (tree-sitter grammar 选择器, 下划线前缀=死代码, v1 纯文本行切片 = 生产路径, AST 定位 P5 未实装) + `use tree_sitter::Parser;` import + 5 tree-sitter 依赖 (tree-sitter/-python/-javascript/-typescript/-rust) 出 `fe-diagnostics/Cargo.toml`; regex = 生产路径不变 (fe-tools 自有 tree-sitter `replace_function` 不动). +3 fe-gui Rust 单元测试 (triple_click/hold_key tag snake_case + holdkey unknown-key 降级 + tripleclick 未授权降级) +2 Python GUI 测试 (NEW_VARIANTS_CI +2 变体降级断言 / triple_click 入 pointer-when-trusted + 新 holdkey-when-trusted TCC 路径). 289 Rust + 153 Python (6 skip TCC) 测试全绿, clippy `--all-targets -D warnings` 净 (仅上游 block v0.1.6), fmt/ruff 净, maturin 构建.

v1.7 COMPLETE (#2 write_file + file_edit create-on-empty): (1) `fe-tools` `write_file(path, content, cwd)` — 整文件原子创建/覆盖 (Claude Code Write parity): `guard_path` 逃逸/敏感防护 → 内容大小上限 `WRITE_FILE_MAX_BYTES` 64MB (防 OOM) → `create_dir_all(parent)` 建父目录 → 已存在则 `FileLock::exclusive` → `atomic_write` (NamedTempFile + persist rename). (2) `file_edit` gate 重写 create-on-empty: 路径不存在 + `old_string` 空 → delegate `write_file(new_string)`; 不存在 + 非空 old → 拒绝 `文件未找到`; 已存在 + 空 old → 拒绝 `不能为空` (L-TOOLS-02 唯一匹配守卫保留). (3) `guard_path` 字面回退分支 bug fix: 父目录不存在时取字面 `abs` 校验 `starts_with(cwd_abs)` — 但 `cwd_abs` 是 canonicalize 后 (`/private/var/...`) 而 `abs` 由 raw cwd (`/var/...`) 构造, macOS tempdir 符号链接 → 嵌套不存在相对路径 `nested/deep/new.py` 误判逃逸; fix = 字面分支用 raw `cwd` (非 `cwd_abs`) 校验. 4 层 wiring (fe-core `write_file` wrapper → fe-ipc `executor.write_file` arm manual `param_str` → fe-pyo3 `#[pyo3(signature=(path,content,cwd=None))]` → Python `FusionSandboxExecutor.write_file`). +6 fe-tools Rust 单元测试 (write_file 3 + file_edit gate 3) +7 Python 测试 (native 6 + UDS roundtrip 1). 295 Rust + 165 Python (1 skip TCC) 测试全绿, clippy `--all-targets -D warnings` 净 (仅上游 block v0.1.6), fmt/ruff 净, maturin 构建. PR #15 merged.

v1.8 COMPLETE (#1 background task API — new `fe-shell` crate, run_in_background/BashOutput/KillShell parity): poll-model persistent shell registry. `ShellRegistry = Mutex<HashMap<String, ShellHandle>>` + `AtomicU64` counter (shell_id = `sh-{N}`). `ShellHandle` 拥有 pid、tail `Arc<Mutex<String>>` (累积尾部上限 `TAIL_CAP=100_000` 防 OOM)、exit `Arc<OnceLock<i32>>`、task_id、command、started_at_ms、finished `Arc<AtomicBool>`. `shell_start(ShellStartParams)` 复用 `fe_sandbox::spawn_pty` (timeout_sec:0.0 unbounded — 后台任务靠调用方 kill_shell, 不走 sandbox heartbeat; use_pty:true) → 自管 `std::thread` reader (4096B 块读, `utf8_safe_prefix_len` 跨块多字节保留不完整字节 — 同 fe-sandbox L-SB-02, `\r\n`→`\n`, 超 TAIL_CAP `drain` 留尾, EOF drop master) + `std::thread` waiter (`child.wait()` 阻塞 → `ExitStatus::exit_code() as i32` (portable-pty 0.8.1 返 u32 非 i32) → `exit.set(code)` + `finished.store(true)`). `shell_output(shell_id)` 轮询 tail 快照 (非增量, 调用方自去重) + running (`!finished.load`) + exit_code; `kill_shell(shell_id)` 复用 `fe_sandbox::kill_tree(pid)` (SIGINT→500ms grace→SIGKILL killpg 返 -124) + 标 finished + `exit.set`; `list_shells()` 全量含已退. **安全**: fe-shell 不做命令校验 — fe-core `shell_start` 前用 `self.security.validate` 拦 (fail-closed; blocked → `ShellStartResult{ok:false, blocked_by_security:true, shell_id:None}` 不调 fe-shell); fe-shell 无 fe-security 依赖. **fe-shell 无 tokio 运行时** (标准 `std::thread` + `std::sync::Mutex` — 避与 BLOCKING_RT 嵌套 panic; reader/waiter 持 `Arc` clone 跨线程). **0 新增 unsafe** (复用 fe-sandbox safe wrapper + portable-pty; fe-shell crate `unsafe_code="deny"`). 轮询模型 (poll-only): 不接 BroadcastHub server-push — live-tail 后续 issue 再补. 4 层 wiring: fe-core (`shell: ShellRegistry` 字段 + 4 wrapper 含安全校验 + `pub use fe_shell as shell` re-export) → fe-ipc (4 dispatch arms manual `param_str` 提取 command/cwd/task_id + `serde_json::from_value` env_vars + defaults max_output_chars=100_000/seatbelt=false/inherit_env=false/max_nproc=1024/max_cpu_sec=0) → fe-pyo3 (3 pyclasses `PyShellStartResult`/`PyShellOutput`/`PyShellInfo` raw serde 透传 + `to_dict`/getters; 4 `#[pymethods]` `shell_start` `#[pyo3(signature)]` + `#[allow(clippy::too_many_arguments)]` env_vars 经 `py.import("json").dumps`→`serde_json::from_str`; `shell_output(&self, py: Python<'_>, shell_id)` — pyo3 0.29 `Python::with_gil` 已移除, 签名传 `py: Python<'_>` 直接 `Py::new(py,...)`; `kill_shell`→`PyResult<bool>`; `list_shells`→`PyResult<Vec<PyShellInfo>>`; 3 `add_class` 在 `#[pymodule]`) → Python (`ShellStartResult`/`ShellOutput`/`ShellInfo` Pydantic `_STRICT` + `FusionSandboxExecutor` 4 方法 + `__init__` re-export). UDS methods: `executor.shell_start`/`shell_output`/`kill_shell`/`list_shells`. +8 fe-shell Rust 单元测试 (returns_id/running_then_finished/kill_long_running/list_records_all/unknown_id_errors/empty_rejects/repeated_output/utf8_multibyte) +1 fe-ipc UDS roundtrip (start→output→kill→list + blocked rm -rf) +12 Python 测试 (native 7: echo+poll/repeated/kill/list/unknown_err/blocked/empty + UDS 5: start/output/kill/list/...). clippy `manual_range_contains` fix (`lead < 0x80 || lead >= 0xC0` → `!(0x80..0xC0).contains(&lead)`). 304 Rust + 176 Python (1 skip TCC) 测试全绿, clippy `--all-targets -D warnings` 净 (仅上游 block v0.1.6), fmt/ruff 净, maturin 构建. 11 crates (新增 fe-shell). PR #16.

**v0.2.1 M-ARCH-1 (architecture refactor):** `ShellRegistry` moved OUT of `Executor` (fe-core) — now owned by the IPC/PyO3 layer (like `BroadcastHub`). `Executor` regains strict per-task statelessness (CLAUDE.md contract): the `shell: ShellRegistry` struct field is removed; `shell_start(&self, registry: &ShellRegistry, p)` keeps the security check on `&self` (security is Executor's concern) but takes the registry by reference; `shell_output`/`kill_shell`/`list_shells` became associated functions `Executor::fn(registry, ...)` (no `&self`) — the stateless contract is now expressed in the type system. `IpcServer` gains a `shells: Arc<ShellRegistry>` field (+ `with_executor_and_shells` ctor, threaded through `accept_loop`→`handle_conn`→`dispatch_request`→`handle_method`→`probe_health_depth`); `PyExecutor` gains `shells: Arc<ShellRegistry>` and `serve()` shares it via `IpcServer::with_executor_and_shells(exec, self.shells.clone())` — so the in-process path and the serve path see the **same** registry (a serve-path `Executor` rebuild no longer drops background `sh-N` handles; fixes P-PYO3-01 invisibility). 0 new unsafe. 339 Rust + 178 Python (1 skip TCC) green, clippy/fmt/ruff clean, maturin builds.

**v0.2.2 M-OPS-01 (structured logging upgrade):** logging moved from fe-pyo3 into a new `fe-ipc/src/logging.rs` module (signal handler for SIGHUP config reload lives in fe-ipc — no cyclic dep). `init_tracing()` builds a JSON `fmt::layer()` (`with_target(true)`) over a `BoxMakeWriter` that **tees** a `tracing-appender` daily-rolling file (`fe.log.YYYY-MM-DD`) with stderr — one fmt layer + one `reload::Layer<EnvFilter>`. Log dir resolves `FE_LOG_DIR` → `~/.fusion-executor/logs/` (private under `$HOME`); unwritable dir degrades to stderr-only (never crashes). `EnvFilter` default `info`, `RUST_LOG` overrides; runtime-reloadable via `FilterHandle::reload_log_filter(directive)` (fail-loud on bad directive, current level unchanged — basis for m-OPS-02 SIGHUP). Idempotent via `OnceLock` (`TRACING_INIT` guards single init; `FIRST_HANDLE` slot lets a late SIGHUP handler re-fetch the handle via `current_handle()`); `WorkerGuard` held in `LOG_GUARD` static so the non-blocking file buffer flushes on exit. `build_subscriber` uses `Registry::default().with(fmt_layer)` + `try_init` (not the `SubscriberInitExt::init` chain — composition with a reload filter needs explicit `Layer::with_filter`). fe-pyo3 `serve()` now calls `fe_ipc::logging::init_tracing()` (local `init_tracing` + `EnvFilter` import removed). Workspace deps added: `tracing-appender 0.2` + `tracing-subscriber "json"` feature. 5 fe-ipc unit tests (FE_LOG_DIR env / home default / FilterHandle reject-bad-directive / accept-valid / current_handle-none-before-init; env tests serialized via `static ENV_LOCK: Mutex`). 344 Rust + 178 Python (1 skip TCC) green, clippy `--all-targets -D warnings` clean (only upstream block v0.1.6), fmt/ruff clean, maturin builds. Smoke: `FE_LOG_DIR=/tmp/x python -c "...serve()"` → `fe.log.2026-08-26` written with JSON lines + stderr dual output, UDS health roundtrip ok, graceful shutdown.

**v0.2.2 M-OPS-02 (Prometheus metrics export):** fe-ipc `BroadcastHub` gains a `prom_handle: Option<PrometheusHandle>` field. `install_prometheus_recorder()` installs the `metrics`-crate global recorder once (idempotent via `static PROM_HANDLE: OnceLock` — returns the cached `PrometheusHandle` on repeat calls so test-time `BroadcastHub::new()` is safe) and registers HELP/TYPE via `metrics::describe_counter!`/`describe_gauge!` for `fe_exec_total`/`fe_exec_success`/`fe_exec_blocked`/`fe_exec_timeout`/`fe_exec_failed`/`fe_rollback_total`/`fe_rollback_failed`/`fe_shell_active`/`fe_connections`. `record_exec`/`record_rollback` now **mirror** the existing `AtomicU64` counters into the recorder via `metrics::counter!(...).increment(1)`; `handle_conn`/`drop_conn` set `metrics::gauge!("fe_connections")`; `shell_start`/`kill_shell` UDS arms recompute active shells (`Executor::list_shells(shells).iter().filter(|s| !s.finished).count()`) and set `metrics::gauge!("fe_shell_active")` (note: `ShellInfo` has `finished: bool`, not `running`). New `BroadcastHub::metrics_prometheus() -> Option<String>` calls `prom_handle.render()` → Prometheus text format; new `executor.metrics_prometheus` UDS arm returns `{ok:true, text:...}` or `-32603` when the recorder failed to install (degrade: JSON `metrics()` still works). **No HTTP port opened** — preserves M-SEC-01 UDS-only security; caller pulls text over UDS and feeds its own exporter. Python `FusionSandboxExecutor.metrics_prometheus(sock_path=None) -> str` wraps the UDS call. Key design note: in-process `run()`/`run_async()` (via `_native.execute_sync`) **bypass** the IPC layer so they are NOT counted — only `executor.execute` over UDS + `shell_start`/`kill_shell` + `snapshot_create`/`rollback` are tracked; this is why the smoke test must use a raw UDS `executor.execute` (not `ex.run()`) to populate the counters. Workspace deps added: `metrics 0.23` + `metrics-exporter-prometheus 0.15` + `arc-swap 1`. +1 fe-ipc unit test (`metrics_prometheus_after_execute` — HELP/TYPE/metric-line asserts) +2 Python tests (`test_metrics_prometheus_over_uds` raw UDS + `test_metrics_prometheus_wrapper_method` wrapper). 345 Rust + 180 Python (1 skip TCC) green, clippy `--all-targets -D warnings` clean (only upstream block v0.1.6), fmt/ruff clean, maturin builds. Smoke: subprocess `serve()` → raw UDS `executor.execute {"echo promtest"}` → `metrics_prometheus()` returns 271 bytes with `# HELP fe_exec_total` / `# TYPE fe_exec_total counter` / `fe_exec_total 1` / `# TYPE fe_connections gauge` / `fe_connections 1`.

**v0.2.2 M-OPS-06 + m-OPS-03 (cross-layer trace_id + log spans):** `ExecutionRequest` gains `trace_id: Option<String>` (`#[serde(default)]`); `ExecutionResult` gains `trace_id: Option<String>` (`#[serde(default, skip_serializing_if="Option::is_none")]`). fe-core `execute_async`/`execute_streaming` resolve `trace_id = req.trace_id.clone().unwrap_or_else(|| uuid::Uuid::new_v4().to_string())` at entry, then enter a `tracing::span!(Level::INFO, "execute"|"execute_stream", trace_id=%trace_id, command=%req.command)` — log lines carry `trace_id=...` span context (m-OPS-03 span markers). `blocked_with` helper gains a 4th `trace_id: Option<String>` param; both blocked paths (security + cwd) and the normal result literals backfill `trace_id: Some(trace_id)`. Streaming captures `trace_id_for_done` before the spawn and writes it on the `Done` frame. uuid v4 dep pre-existed in workspace (no new dep). 4-layer wiring: fe-core serde struct → fe-pyo3 `PyExecutionResult.trace_id` (`#[pyo3(get)]` + `From`) + `execute_sync`/`execute_streaming` `#[pyo3(signature=(..., trace_id=None))]` → Python `ExecutionRequest.trace_id`/`ExecutionResult.trace_id` Pydantic (`extra=forbid`) + `run()`/`run_streaming()` kwargs → fe-ipc done frame **auto-flows** via serde flatten (Python `_native_result`'s `model_validate(fields)` picks up additive field, no per-field wiring). `shell_start` deliberately NOT modified — trace_id is `ExecutionRequest`-scoped per audit; background shells have no `ExecutionResult` to carry it (Rule 2). +4 fe-core Rust tests (auto-gen uuid v4 shape / forwarded / blocked carries it / streaming done carries it) +4 Python tests (auto-gen / forwarded / blocked / streaming done). 349 Rust + 180 Python (1 skip TCC) green, clippy `--all-targets -D warnings` clean (only upstream block v0.1.6), fmt/ruff clean, maturin builds.

**v0.2.2 m-OPS-02 (SIGHUP config hot-reload):** SIGHUP to the running server (`kill -HUP <pid>`) reloads **log level** + **whitelist** without restart. fe-ipc `handle_sighup_reload(executor)` — a dedicated async loop (parallel to `wait_signal_and_notify` SIGINT/SIGTERM; SIGHUP is reload-only, **never** calls `hub.shutdown.notify_waiters()`), spawned in both `serve()` async + `serve_blocking`, aborted on accept-loop exit. Two reload fns: `reload_log_level()` reads `RUST_LOG` (unset/empty → `info` default), applies via `logging::current_handle()` → `FilterHandle::reload_log_filter` (fail-loud on bad directive; `None` handle → `WARN` skip, never panic); `reload_extra_whitelist(&Arc<Executor>)` reads `FUSION_EXECUTOR_EXTRA_WHITELIST` (comma-split, trim, drop-empty) → `executor.reload_whitelist(&[&str])`. fe-security: `SecurityGuard.whitelist` changed `HashSet<String>` → `ArcSwap<HashSet<String>>` (interior mutability — `store(Arc<T>)` takes `&self`, `load()` lock-free read, **avoids `&mut self` ripples through all `validate` call sites**; `Executor` stays stateless per-task per M-ARCH-1). Removed `#[derive(Clone)]` (ArcSwap doesn't impl Clone) → manual `impl Clone` clones inner set via `ArcSwap::from_pointee((**self.whitelist.load()).clone())`. New `reload_extras(&self, extras)` rebuilds **from baseline** (`WHITELIST` const) + `merge_into` (NOT accumulate — discards prior extras, baseline always present); `DENY_EXTEND` rejects dangerous interpreters (`sh`/`bash`/`zsh`/`exec`/`eval`/`sudo`/...). `with_extra_whitelist` (builder) keeps accumulate semantics via `merge_whitelist`; `validate` reads `self.whitelist.load().contains(...)`. 0 new unsafe (ArcSwap is safe; fe-security keeps `unsafe_code="deny"`). arc-swap dep added to fe-security (already workspace dep). +3 fe-security tests (reload replaces not accumulates / empty clears extras keeps baseline / rejects dangerous interpreter) +4 fe-ipc tests (reload_extra_whitelist parses env / empty clears extras / reload_log_level no-panic-without-handle / reads RUST_LOG; env tests serialized via `static MOPS02_ENV_LOCK: Mutex`). 356 Rust + 184 Python (1 skip TCC) green, clippy `--all-targets -D warnings` clean (only upstream block v0.1.6), fmt/ruff clean, maturin builds. Smoke: `kill -HUP $(pgrep -f fusion_executor)` with `RUST_LOG=debug` → log level flips; `FUSION_EXECUTOR_EXTRA_WHITELIST=jq,gh` → `jq --version` allowed (was blocked). Deferred-ops sub-batch (#86-89) COMPLETE.

**v0.2.3 #20 (glob E1 spec alignment):** `fe-tools` `glob()` 对齐 fusion-event E1 生态统一 glob 规范 (`fusion-event/docs/glob-spec.md`)。**Bug fix**: globset 默认 `*` 跨 `/` 与 E1 冲突 — `src/*.swift` 误命中 `src/sub/a.swift`。修法: `globset::Glob::new(pattern)` → `GlobBuilder::new(pattern).literal_separator(true).build()`, 收紧 `*`/`?` 不跨 `/`, `**` 仍跨目录 (literal_separator 仅影响单段通配, 不影响 `**` 递归)。spec 权威在 fusion-event (他工程, 只读), fusion-executor glob 实现对齐其语义; 模式匹配相对 cwd 路径 (`strip_prefix(cwd_abs)`), 故 spec 绝对模式 (`/src/*.swift`) 在此适配为相对 (`src/*.swift`)。3 E1 验收测试 (`test_glob_e1_star_within_segment` `src/*.swift` 命中同层不跨 / / `test_glob_e1_doublestar_across_dirs` `src/**/*.swift` 跨目录 / `test_glob_e1_question_one_char` `bin/?s` 单字符不命中 `less`) — **测试先于修复失败暴露真 bug** (Rule 9), `literal_separator` 后全绿。既有 `**/*.py` 跨目录测试保持绿 (literal_separator 不破坏 `**`)。0 新增 unsafe, 0 新依赖。359 Rust + 184 Python (1 skip TCC) green, clippy `--all-targets -D warnings` 净 (仅上游 block v0.1.6), fmt/ruff 净, maturin 构建.

**Audit 0827 adversarial hardening (P0-P3 全落地, branch `fix/audit0827-p0-p3`):** 6 路对抗性审计 (`audit/fusion-executor-audit-0827.md` 只读参考, 基线 `deae2c3` v0.2.3) 查 67 findings — 18 CRITICAL (C-1..C-18) + 18 LOGIC-BUG (L-1..L-18) + 12 ARCHITECTURE (A-1..A-12) + 7 PERFORMANCE (P-1..P-7) + 12 MAINTAINABILITY (M-1..M-12, P4 bundled)。12 crate-cluster task #94-#105 全绿:
- #94 fe-security (C-1 `&`/redirect 绕过 — tokenizer 视 `&` 为 compound 分隔符非 background; C-5/C-6/C-11/C-12/C-13 GUI 越权 scope; A-7; L-4/L-5; P-3)
- #95 fe-sandbox (C-14 `setsid` 新进程组隔离; C-15 `kill_tree` killpg 递归 reap 无孤儿; C-16 seatbelt SENSITIVE_FS_PATHS file-write deny; A-10/A-11/A-12; L-13)
- #96 fe-diagnostics (C-17 ReDoS cap + 编译超时 + 长度上限; C-18 symlink TOCTOU canonicalize-on-open; L-16/L-17/L-18; P-6; A-8; M-10)
- #97 fe-ipc (C-8/C-9/C-10 资源泄漏 — rx-drop abort + saw_done guard + graceful shutdown; L-6/L-7; P-1/P-2; A-6; M-7)
- #98 fe-gui (C-13; M-12.3/M-12.4)
- #99 fe-core (C-2/C-4; L-2/L-3; A-5)
- #100 fe-pyo3 (C-3/C-4 streaming recv timeout + saw_done)
- #101 fe-rollback (C-7 capture_wip fail-loud; L-1 `RollbackOutcome{applied,skipped_reason,wip_sha}` + `rollback_skipped_reason` 4 层; L-8 `rollback_file` RepoLock)
- #102 fe-tools+security (L-9 check_size 移锁内; L-10 fail-closed canonicalize; L-11 split peek-ahead; L-12 Insert missing-id 报错非静默 append; A-1; M-12.1/M-12.2)
- #103 fe-shell (P-4 kill 外锁 / P-5 join 外锁 / M-8 reader catch_unwind / M-9 kill_grace_ms 4 层) + fe-telemetry (L-15 `pid` 字段 / A-9 channel64 + `tx.is_closed()` 防挂死 — **真 bug**: 进程消失路径 `continue` 未查 rx drop → 无限 10ms sleep 循环 → handle 永不完成 → test hang >60s; fix = `tx.is_closed()` check → break / P-7 `new_all` baseline + warn 跳帧 / M-11 `sanitize_gpu`)
- #104 Python (P-2 `Subscription._buf` 8MB OOM cap; M-4 native ImportError `warn` fail-visible; M-5 cli `_validate_paths` 启动期 fail-fast sys.exit(2); M-6 `run_streaming` 未知帧 type debug 日志)
- #105 workspace verify + docs
419 Rust + 190 Python (1 skip TCC) 全绿, clippy `--all-targets -D warnings` 净 (仅上游 block v0.1.6 future-incompat), fmt/ruff 净, maturin 构建. 提交链 #94-#105 on `fix/audit0827-p0-p3` (不 push 待命).

**v0.2.4 enterprise-publishable upgrade (产品就绪审计, branch `fix/enterprise-upgrade-0828`):** 产品就绪审计 (`audit/fusion-executor-audit-result-product-0827.md` 只读参考, 基线 `9b96aeb` v0.2.3/PR #25) 判定 ❌ 不可企业级商用 (6 真 CRITICAL + 33 MAJOR/MINOR) → 升级至企业级可发布, 16 批次全落地:
- D3-1 内联解释器网关: `fe-security::validate_argv` 默认拦截 `python -c`/`python -m`/`node -e`/`ruby -e`/`perl -e` (binary+flag 级, payload 内容无关 — 防 agent-driven 模型生成 one-liner 绕 regex 危险词枚举); `with_allow_inline_interpreter(true)` opt-in 受信调用方。Stage1 顶层 regex → tokenizer → Stage2 白名单 → argv 网关 (4 层防御纵深: 危险词 payload 由 Stage1 拦, 非白名单解释器由 Stage2 拦, 白名单解释器 + -c/-e 由网关拦)。`d311_blocks_inline_exec_payload_variants` 回归测试隔离测网关本身 (白名单解释器 + 无害 payload)。
- ARCH-1 seatbelt 默认 True 4 层: `fe-core` serde `default_true` + `executor.py` run/run_streaming/shell_start 默认 True + Pydantic `ExecutionRequest.seatbelt` default True (D1-2/D2-3 drift 修 — Pydantic 旧默认 False 漂移, 已对齐 True) + Rust serde。`test_seatbelt_default_true_all_layers_no_drift` drift 断言锁。
- ARCH-2 resolved-path 白名单: `fe-security::resolve_binary_path` 双校验 — (1) basename 在白名单; (2) 绝对路径 `starts_with(trusted_bin_dirs)` (默认 `/usr/bin`/`/usr/local/bin`/`/opt/homebrew/bin` + venv bin 自动注册)。`/tmp/python3` 同名投毒 → 拒 (D3-6 fail-closed)。`Executor` auto-register `VIRTUAL_ENV/bin` trusted dirs。
- D3-4 每任务 RSS watchdog: `fe-sandbox` sysinfo 轮询子进程树 RSS, 超阈值 kill (exit -124, `oom_killed=true`); Darwin RLIMIT_AS no-op 故改 watchdog 非 rlimit。
- RUN-4/RUN-10 rlimit: seatbelt profile + 非 seatbelt 路径 `setrlimit` NOFILE(1024)/NPROC(512)。
- RUN-9 ppid 树 kill: `kill_tree` killpg + sysinfo ppid 树遍历兜底 (防孙进程 setsid 脱组逃逸)。
- RUN-3 stream/exec 信号量分离: `fe-ipc` `exec_sem=16` (非流 execute) + `stream_sem=64` (流式), 长流不饿死短命令。
- RUN-6 git 超时 + RUN-7 worktree gitdir: `fe-rollback::git()` `tokio::time::timeout(30s)` + `kill_on_drop`; `RepoLock::acquire` 不跨 `.await` (仅保瞬时状态读); `resolve_git_dir` `rev-parse --git-common-dir` (worktree `.git` 是文件 ENOTDIR bug 修)。
- RUN-5 NFS 检测: `fe-tools::is_nfs` stat fstype, NFS mount → warn (不引分布式锁, Rule 2 — 当前单机 UDS 无 NFS 部署)。
- IMPL-7 prompt-injection sanitize: `fe-diagnostics::slice()` 单一 chokepoint — `sanitize_file_path` (换行切分) + `sanitize_raw_trace` (inject_re→[filtered] 中和) + `sanitize_error_type`, 大小上限 1KB/4KB/256B。
- RUN-12 默认 bundle 白名单 + IMPL-9 截图 TCC 分离: `fe-gui` `GuiConfig::default()` 非空安全集 (Terminal/TextEdit/Finder), `disable_bundle_allowlist` opt-in 无限制; screenshot `gui_action(Screenshot)` 走 Screen-Recording TCC (与 Accessibility 独立, early-return before AX gate)。
- RUN-11 子进程 pid 遥测: `fe-telemetry` 沙箱执行自动采 child pid (非 executor pid), 4 层 surface。
- IMPL-1 默认 socket + IMPL-3 Subscription 崩溃标记: Python `DEFAULT_SOCK=~/.fusion-executor/fe.sock` (4 层对齐 M-SEC-01); `Subscription.__next__` 断连区分正常结束 vs server 崩溃 → `ConnectionError` 非 StopIteration。
- D4 perf: `LazyLock` profile cache (D4-1) + 16KB 读取缓冲 (D4-5) + 广播 fast-path (D4-4) + exec_sem gauge (D4-9) + worker clamp(2,8) (D4-12)。
- ARCH-3 集成骨架: `examples/08_integrate_fusion_code.py` (本工程内单向消费 executor, 不 import fusion-code) + fusion-code 单向 issue (ARCH-7 调用方熔断 `max_consecutive_failures` 文档化)。
- D1-1 fe-pyo3 首批 Rust 单元测试: `PyExecutionResult::From` 16 字段映射 + `PyEditResult`/`PyGlobEntry`/`PyGrepMatch` From 转换覆盖。
- IMPL-8 DISPROVEN: pyo3 0.29 trampoline 已 `catch_unwind`, 无需 wrapper。
- annotate (D3-7/D4-7): seatbelt allowlist-by-default rationale + grep per-call regex rebuild µs-level rationale。
**出口: 476 Rust + 195 Python (6 skip TCC) 测试全绿**, clippy `--all-targets -D warnings` 净 (仅上游 block v0.1.6 future-incompat), fmt/ruff 净, maturin 构建. 跨工程 Issue #23 (fusion-guard Phase 3) 延期 — 超范围。提交链 16 批次 on `fix/enterprise-upgrade-0828`, 合并至 main, tag `v0.2.4` + GitHub release。

See `architecture/fusion-executor-prd.md` §4 for the full `ExecutionRequest` / `ExecutionResult` field list and the Diagnostics Slicer algorithm.

## Build / Test / Lint

Rust core + Python bindings via **maturin/PyO3** (first such project in the monorepo — deliberate divergence from setuptools). Shared venv is Python 3.14 → requires pyo3 ≥0.29.

```bash
cd /Users/dahai/fusion
source .venv/bin/activate          # shared venv at repo root — REQUIRED first
cd fusion-executor

# Build & install native extension (editable) into shared venv
maturin develop --release

# Rust
cargo check --workspace
cargo test --workspace                  # e.g. cargo test -p fe-security
cargo clippy --workspace -- -D warnings
cargo fmt --all -- --check

# Python
pytest python/tests                     # asyncio_mode=auto, testpaths=["python/tests"]
pytest python/tests/test_executor.py::test_run_echo -v   # single test
ruff check . && ruff format .           # py311, line-length 120

# Smoke
python -c "from fusion_executor import FusionSandboxExecutor; print(FusionSandboxExecutor().run('echo hi').exit_code)"

# Start UDS JSON-RPC server (P3)
python -c "from fusion_executor import FusionSandboxExecutor; FusionSandboxExecutor().serve()"
# Socket: ~/.fusion-executor/fe.sock (HOME-private 0o700; override FUSION_EXECUTOR_SOCK)
```

Python ≥3.11 (venv is 3.14). Runtime dep `pydantic>=2.0`. Test deps `pytest`/`pytest-asyncio`/`pytest-cov`. No httpx, no hard fusion-core — executor is an L4 OS tool, delegates inference to caller. Package root is `fusion_executor` (import as `from fusion_executor import FusionSandboxExecutor, ExecutionResult`). Native extension is `fusion_executor._native` (built from `crates/fe-pyo3`).

## Integration With Ecosystem

- **fusion-code → fusion-executor**: fusion-code drops generated patch to disk, calls `executor.run("pytest tests/")`, receives structured diagnostics, enters next self-healing loop iteration. Refactor plan (PRD §"重构"): fusion-code strips its own subprocess/file-IO into an `ExecutorDriver` interface — all command validation, timeout, stdio capture delegated here. **ARCH-7 caller-circuit contract (audit 0827)**: `RollbackPolicy.max_consecutive_failures` is a reserved field the stateless `Executor` never reads — the **caller** (fusion-code self-healing loop) owns the consecutive-failure count and reads that field as its circuit-breaker threshold; auto-rollback is per-execute, not per-loop. Reference skeleton: `examples/08_integrate_fusion_code.py` (consumes the executor API only, does not import fusion-code; one-way issue opened on fusion-code for the `ExecutorDriver` refactor).
- **fusion-executor → fusion-studio**: live stdio stream, screenshot sampling, GPU/CPU telemetry broadcast over Unix Domain Socket to the studio dashboard (zero-copy, high-frame-rate render).
- **fusion-executor ↔ fusion-mlx / fusion-gateway**: UDS comms (not HTTP/gRPC) — terminal Traceback-to-model-prompt transfer latency target <2ms.
- **Replaces**: Claude SDK's BashTool/FileEdit/Glob/Grep + Docker sandbox, and DeepSeek Harness's SWE-bench container — but native (no Docker): macOS process isolation + Git snapshots, sandbox init overhead target <5ms.

## Key Design Constraints (NFRs / SLA)

- CLI sandbox init overhead <5ms; log-truncation + regex parse CPU <3% under high throughput.
- Guard against chain-assembly bypass — full token-level parse over `&&`, `||`, `;`, `|`.
- Cap memory on infinite-print death-loops — prevent OOM.
- Exit-code convention must stay stable: `0` success, `-124` timeout, `-1` blocked/security/internal.

## Monorepo Conventions (apply here)

- Indentation: multiples of 4 spaces. No docstrings. Always include logging.
- Domain apps use MLXClient dependency injection + `_parse_json()` for LLM output — but this is an execution tool, not a domain app; it calls OS, not the model. If it ever needs inference, go through `fusion_core.mlx_client.FusionMLXClient` (never raw `httpx` to fusion-mlx — P1-8 violation pattern).
- IPC to fusion-studio uses JSON-RPC 2.0 over Unix Domain Socket.
- Build backend: **maturin** (PyO3) — `crates/fe-pyo3` produces `fusion_executor._native`. NOT setuptools; the only PyO3 project in the monorepo.
- GUI (fe-gui) tests are **manual** — AXUIElement/CoreGraphics need TCC Accessibility + Screen Recording permission; CI skips GUI when `!AXIsProcessTrusted()`.
- `ExecutionResult.diagnostics` field is additive over PRD §4.1 (approved) — delivers PRD §4.2 Slicer output. Live stdio streaming (PRD §5) landed in v1.2 — `run_streaming()` yields chunk strings then `ExecutionResult`; fe-sandbox `run_streaming` → fe-core `execute_streaming` (mpsc `ExecutionStreamEvent` `Chunk`/`Done`) → fe-ipc `executor.execute_stream` (NDJSON multi-frame) → fe-pyo3 `NativeStreamIterator` → Python generator. Rollback is **caller-driven by default** (executor exposes `snapshot_create`/`rollback`, stays stateless per-task); v1.4 adds optional `auto_rollback=RollbackPolicy{...}` kwarg on `run()`/`run_streaming()` — executor auto-rolls-back to the pre-exec snapshot on `exit_code!=0` + git-status-detected file damage, sets `result.auto_rolled_back=True`, but still does NOT track consecutive-failure counts (that stays with the caller's self-healing loop).
- Upstream problems (fusion-mlx, fusion-gateway, fusion-core): file issue first, then PR, follow up with code — don't patch other projects in-tree.

## Key Paths

- `architecture/fusion-executor-prd.md` — full PRD, architecture diagram, data schema, diagnostics algorithm, fusion-code refactor plan, Claude-SDK/DeepSeek-Harness capability comparison.
- `audit/fusion-ar-audit.md` (P2-7) — rationale for extracting fusion-science's code sandbox here.
- `/Users/dahai/fusion/CLAUDE.md` — monorepo overview, shared `.venv`, fusion-mlx lifecycle (`~/claude-home/fusion-mlx/start.sh start|stop|status`).
